# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Generated by openapi-typescript from ../openapi.json. Not formatted here on purpose: the CI drift
# check compares the generator's raw output against the checked-in file, so running Prettier over it
# would make that comparison depend on two tools agreeing about formatting forever.
src/lib/api/schema.d.ts
